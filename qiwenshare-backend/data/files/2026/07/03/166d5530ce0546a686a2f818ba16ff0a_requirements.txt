# RAG 教学系统依赖
# 必须安装
numpy>=1.21.0

# 可选：高质量多语言嵌入（安装后自动启用，替代 TF-IDF）
# pip install sentence-transformers
# sentence-transformers>=2.2.0

# 可选：接入真实 OpenAI LLM（需要 API Key）
# pip install openai
# openai>=1.0.0

# 可选：运行单元测试
# pip install pytest
# pytest>=7.0.0
# requirements.txt
# ============================================================
# RAG 教学系统依赖说明
#
# 必须安装（核心依赖）：
#   numpy：向量运算的基础库，已在大多数环境中预装
#
# 可选安装（高质量嵌入）：
#   sentence-transformers：神经网络嵌入模型，支持中文
#   安装命令：pip install sentence-transformers
#   首次运行会自动下载模型（约 400MB）
#
# 可选安装（真实 LLM）：
#   openai：接入 OpenAI GPT API
#   安装命令：pip install openai
#
# 可选安装（运行测试）：
#   pytest：测试框架
#   安装命令：pip install pytest
# ============================================================

# 必须安装
numpy>=1.20.0

# 可选：高质量嵌入（推荐安装，支持中文语义搜索）
# sentence-transformers>=2.0.0

# 可选：接入真实 OpenAI API
# openai>=1.0.0

# 可选：运行测试
# pytest>=7.0.0
